import React from 'react'
import ReactDOM from 'react-dom/client'
import ReplitFinderApp from './App.replitfinder.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReplitFinderApp />
  </React.StrictMode>,
)